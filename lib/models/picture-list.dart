class PictureList {
  String pictureId;
  String pictureTitle;
  String pictureImage;
  DateTime pictureDate;

  PictureList(
      {this.pictureDate, this.pictureId, this.pictureImage, this.pictureTitle});
}
